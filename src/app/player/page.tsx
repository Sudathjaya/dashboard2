"use client";
import Grid from "@mui/material/Grid2";
import PlayerDashboard from "./Player";

const TeamPage = () => {
  return (
    <Grid>
      <PlayerDashboard />
    </Grid>
  );
};

export default TeamPage;
